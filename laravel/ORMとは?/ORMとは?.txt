#ORmとは？

	ORMはSQLを書かなくていいもの
	オブジェクトのようにDB操作ができるようになる
	SQLを書かずともDB操作ができる


	PDOオブジェクトでDB接続の関数を作って、
	SQLを書きプレイスホルダーして　try catchでデータを流し込み
	
	クエリを実行する　という流れだった

	
	しかし
	
	$hoge = new Hoge;
	$hoge->fill($request->all())->save();

	
	
 	の２行で済む。
	
			



	ただ、SQLを覚えなくてもいい訳ではなく
	大規模になるほど複雑なDB操作を求められるため、そうなるとSQLをかけなけれ		ば難しく　実務でも必要？？	
